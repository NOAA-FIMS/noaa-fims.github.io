name: Accessibility Audit

# Trigger this on Pull Requests against main, and allow manual runs
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  accessibility-audit:
    runs-on: ubuntu-latest
    env:
      R_REMOTES_NO_ERRORS_FROM_WARNINGS: true
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      # Setup R and Quarto exactly like your deployment workflow
      - uses: r-lib/actions/setup-r@v2
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          packages:
            any::knitr
            any::rmarkdown

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Render Quarto Project
        uses: quarto-dev/quarto-actions/render@v2
        with:
          to: html

      - name: Run Accessibility Audit
        continue-on-error: true
        id: a11y_audit
        env:
            PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: "false"
        run: |
            # Install serve, pa11y-ci, and the HTML reporter
            npm install -g serve wait-on pa11y-ci pa11y-ci-reporter-html
            
            # Serve the site locally
            serve -p 3000 _site &
            wait-on http://localhost:3000
            
            # Generate URLs for all generated HTML files
            find _site -name "*.html" | sed 's|^_site/|http://localhost:3000/|' > local-urls.txt
            
            mkdir -p a11y-reports
            
            # Create pa11y config with Puppeteer sandbox disabled
            cat > .pa11yci.json << 'EOF'
            {
                "runners": [
                "axe",
                "htmlcs"
                ],
                "chromeLaunchConfig": {
                "args": [
                    "--no-sandbox",
                    "--disable-setuid-sandbox",
                    "--disable-gpu"
                ],
                "ignoreHTTPSErrors": true
                }
            }
            EOF
            
            # Run Pa11y using the HTML reporter and save it to a file
            PA11Y_CI_REPORTER=html pa11y-ci --config .pa11yci.json $(cat local-urls.txt) > a11y-reports/accessibility-report.html
            
            # Run again strictly to capture the exit code for GitHub Actions Pass/Fail status
            pa11y-ci --config .pa11yci.json $(cat local-urls.txt)

      - name: Upload Accessibility Report
        uses: actions/upload-artifact@v4
        with:
          name: Accessibility-HTML-Report
          path: a11y-reports/accessibility-report.html
          retention-days: 14

      - name: Fail workflow if audit failed
        if: steps.a11y_audit.outcome == 'failure'
        run: |
          echo "Accessibility violations found. Please download the 'Accessibility-HTML-Report' artifact from the Actions tab for details."
          exit 1